Every expectation must match the same event;

Unrelated events are ignored;

Zero matches means failure;

One or more matches means success;

We assume that supplied events belong to one scenario run;

A forbidden field in the supplied events causes verification to fail, even when another event matches the contract;

