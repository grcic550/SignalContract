# MOOC Part 2: All together

**Starting Date:** July 30, 2026  
**Completed Tasks:** 5 or 6 tasks (up to [Name of last task you did])

### 1. Core Concepts I Learned
In starting Part 2 so far I have learned about Statements, Expressions, Functions, Blocks, data types and syntax. Statement is a part of program that executes something. for example: print("Hi!"). Block is a group of consecutive statements that are in the same level in the structure of the program. Expression is a bit of code that results in determined data type. For example arithmetic expression: 2+3+4

Function executes some functionality. They can also take one or more arguments, which are data taht can be fed and processed by a function. Data type refers to the characteristics of any value present in the program. Syntaxy of the program defines how the code of the program should be written. Each programming language has its own specific syntax.

If the syntax of the program is correct but the program still doesn't function as intended , then there is a bug in the program. Bugs manifest in a lot of ways. It is useful to add print statements to see bugs.

Instead of creating multiple if statements in some case else statement is fine.

Here I learned mostly about contitional operations which I already knew so I experimented a bit with "match" which is equivalent to sqitch in C and also with .lower() function.

i learned about nested conditionals. I learned about the loops and how to use them. Learned how to break out of them. I tried to write the code in the most efficient way possible but sometimes it wasn't the easiest.

### 2. One Mistake I Made
In solving the exercises for the first part I mostly solved them wrong but I tried to get the decimal part of the number by doing it like this: number%int(number) and it said that it divided by zero so I don't know if it is correct.

As I said, I tried to write code optimally with as few variables as I could and tried to use logic to do so. In some of the tasks, maybe one or two, I didn't manage to do so. I used the help of claude. It wasn't for the whole task it was just how to use .split()[-1] function since I was unfamiliar with it.I wanted to use it to not create any more unecessary variables.

### 3. Next Small Step
- Next is going on to the Part 3 of MOOC and in parallel on HTB.