# MOOC Part 4: All together

**Starting Date:** August 13, 2026  
**Completed Tasks:** All of them

### 1. Core Concepts I Learned
This week, I learned about several different things, starting with how to use Visual Studio Code to make writing code easier. I also learned about new functions that I can use, such as `min()`, `max()`, `sum()`, and `count()`.

I learned about lists and some functions and methods that can be applied to them, such as `pop()`, `remove()`, and `append()`. I also learned how to access elements from lists using indexes.

I learned about definite iteration and used `for` loops for most of the exercises. If there was a mistake or the program did not work as expected, I learned how debugging can help find and fix the problem.

I also learned about the `sort()` method, which is useful for sorting lists, and the `range()` function, which is useful when working with definite `for` loops.

Finally, I learned that strings are immutable, which means that they cannot be changed in the same way that lists can. You can create a new string by adding something to an existing string, but you cannot directly change the existing string itself.


### 2. One Mistake I Made
I didn't make one mistake, I made multiple especially in this part where tasks become more challenging. Although I made a lot of mistakes, some of the tasks werent that hard because I was introduced to a lot f methods and functions that helped to perform tasks automatically
### 3. Next Small Step
- Next is going on to the Part 5 of MOOC and in parallel on HTB.