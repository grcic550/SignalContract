# MOOC Part 9: All together

**Starting Date:** August 31, 2026  
**Completed Tasks:** All of them

### 1. Core Concepts I Learned
This week I learned about class hierarchies, including inheritance, and I learned how to use `super()`. I also learned about protected variables, such as `_variable`, and private variables, such as `__variable`. The same principles apply to methods within a class.

I also learned about built-in operators for classes, such as `==`, `>`, and `<`, and how they can be implemented using special methods such as `__lt__`, which is an example for the "less than" (`<`) operator.

I learned about managing complexity by breaking larger tasks into smaller, more manageable sections and working on them individually. I also learned that when wondering what should be represented as an object, real-world objects are often good candidates for objects in a program.



### 2. One Mistake I Made
There are no things that are unclear in a theoretical sense, but it is still about practice and finding patterns for solving tasks. The mental work is what remains. Nothing in particular. Maybe in bigger tasks, explaining the limitations a bit more would be helpful.

### 3. Next Small Step
- Next is going on to the Part 11 of MOOC and in parallel on HTB.